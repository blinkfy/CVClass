第四次实验关键技术说明

一、卷积类边缘检测算法

系统实现了多种基于卷积核的边缘检测方法，包括 Sobel、Prewitt、Roberts、Scharr、Kirsch、Laplacian 和 LoG / Marr 等。核心思想是使用不同方向或不同类型的卷积核在灰度图像上滑动，对局部区域进行加权求和，从而突出灰度变化明显的位置。

系统中的二维卷积由本人手写实现，基本过程为：

1. 根据卷积核大小对图像进行边界填充；
2. 按照 stride 在图像上滑动卷积窗口；
3. 取出当前局部 patch；
4. patch 与 kernel 对应位置逐元素相乘；
5. 将乘积结果累加，得到当前位置的响应值；
6. 将所有位置的响应值组成边缘响应图。

其中 Sobel、Prewitt、Scharr 等方法分别计算水平方向和垂直方向梯度，再通过梯度幅值合成最终边缘强度：

G = sqrt(Gx² + Gy²)

对于 Kirsch 算子，系统使用八个方向模板分别计算方向响应，并取最大响应作为最终边缘强度。对于 Laplacian 和 LoG 算子，系统使用二阶导数思想检测灰度突变区域。

二、Canny 边缘检测算法

Canny 算法是本实验的重点之一。系统按照经典 Canny 流程手写实现完整计算过程，包括：

1. 高斯平滑
   使用高斯核对灰度图进行卷积，降低噪声对边缘检测的影响。

2. 梯度计算
   使用 Sobel X 和 Sobel Y 卷积核计算水平方向和垂直方向梯度。

3. 梯度幅值与方向计算
   根据 Gx 和 Gy 计算梯度幅值和梯度方向。系统支持 L1 梯度近似和 L2 梯度计算方式。

4. 非极大值抑制
   沿梯度方向比较当前像素与相邻像素的梯度幅值，只保留局部最大响应，使粗边缘变细。系统实现了近似方向分类版本，并扩展了基于插值的精确比较版本。

5. 双阈值分类
   根据 high threshold 和 low threshold 将像素划分为强边缘、弱边缘和被抑制区域。

6. 滞后连接
   从强边缘出发，搜索 8 邻域中的弱边缘。若弱边缘与强边缘连通，则保留；否则删除。该过程通过栈或队列实现连通搜索。

系统没有调用 OpenCV 的 cv2.Canny，也没有调用 skimage 的 Canny 封装函数。Canny 中的高斯平滑、梯度计算、非极大值抑制、双阈值和滞后连接均由本人根据算法原理手写完成。

三、算法过程可视化技术

为了展示算法过程，系统不仅显示最终结果，还将边缘检测拆解为可观察的阶段。卷积核算子页面展示 Image、Gray、Gx、Gy、Magnitude、Threshold、Final 等步骤；Canny 页面展示 Image、Gray、Gaussian Blur、Gradient、Direction、NMS、Double Threshold、Hysteresis 和 Final 等阶段。

卷积核算子页面进一步设计了局部卷积计算动画，通过点击图像像素展示：

Patch × Kernel → Product → Sum → Response → Threshold

页面使用颜色高亮、连线、局部矩阵和计算卡片展示每个像素点如何由局部卷积得到响应值，再经过阈值判断成为边缘点或非边缘点。

Canny 页面通过流程时间线和步骤卡片展示多阶段检测过程。不同阶段对应不同说明，例如 Gaussian Blur 表示平滑降噪，NMS 表示沿梯度方向保留局部最大响应，Double Threshold 表示强弱边缘分类，Hysteresis 表示从强边缘出发连接弱边缘。

四、方法对比与参数交互

系统支持对多种边缘检测方法进行统一比较。用户可以上传图片或选择示例图片，点击一键生成对比后，系统会分别计算 Roberts、Sobel、Prewitt、Kirsch、Laplacian、LoG / Marr、Canny、TEED / HED 方法的边缘结果，并显示每种方法的耗时、边缘像素占比和算法类别。

方法对比页面提供滑块对比功能，可以将原图与某一种边缘检测结果左右拖动对比，也可以比较不同算法之间的差异。右侧说明区会根据当前选中的算法动态显示其类别、特点、优点、缺点和适用场景。

五、边缘检测应用实践

系统扩展了“应用实践”页面，用于展示边缘检测结果的后续应用。该页面主要使用前端 JavaScript 和 Canvas 完成计算与动画展示，包括轮廓提取、Hough 直线检测和 Hough 圆检测等功能。

轮廓提取部分基于边缘图进行连通域分析，通过 4 邻域或 8 邻域搜索将相邻边缘点归为同一轮廓，并计算轮廓数量、最大轮廓大小、外接矩形和被过滤噪声区域。页面还设计了轮廓剪裁功能，可以点击某个轮廓，从原图中裁剪对应像素区域，并在工作区中生成可拖拽对象，用于展示边缘检测在目标提取和图像编辑中的实际应用。

Hough 直线检测部分通过参数空间投票实现。对于每个边缘点，系统根据公式:

rho = x cos(theta) + y sin(theta)

遍历不同 theta，将投票累加到 Hough accumulator 中。当累加器中出现峰值时，峰值对应图像中的一条直线。页面通过边缘点高亮、参数空间曲线绘制、峰值标记和直线回投影展示 Hough 变换过程。

Hough 圆检测部分基于圆方程：

(x - a)² + (y - b)² = r²

对可能圆心进行投票，并将检测到的圆叠加回原图显示。为保证前端性能，该部分可以使用固定半径或小范围半径扫描作为教学演示。

六、TEED / HED 深度边缘检测拓展

系统还增加了 TEED / HED 深度边缘检测拓展页面，用于展示深度学习边缘检测中的 Stage Feature、Side Output 和 Fusion 思想。该页面通过 Original、Stage Feature、Side Output 和 Fusion / Final Edge 展示深度模型如何从不同层级提取边缘特征，并将多个侧输出融合为最终边缘概率图。

需要说明的是，TEED / HED 页面属于拓展展示内容，不属于本实验要求的 Sobel 和 Canny 手写核心算法范围。该页面用于对比传统边缘检测与深度学习边缘检测的思想差异，说明深度模型可以通过多尺度特征和融合输出获得更丰富的边缘表达。

七、性能优化

系统在部分计算量较大的场景中采用了性能优化方法。例如，后端边缘检测核心函数可以使用 Numba 对循环计算进行加速。对于较大的图像，系统会将图像缩放到适合演示的分辨率，保证浏览器端计算和动画播放流畅。

在前端动画部分，系统使用 Canvas、SVG、CSS animation 和 requestAnimationFrame 实现步骤播放、流光连线、矩阵高亮、轮廓扩展、Hough 投票和结果叠加等效果。这样可以在不引入复杂前端框架的情况下，实现较完整的教学型过程可视化。

八、代码边界说明

本系统中 Sobel、Canny 以及其他传统边缘检测核心算法由本人根据课程原理手写实现，未调用 OpenCV、skimage 等库中的边缘检测封装函数。AI 辅助用于页面结构、样式、动画展示和交互界面的生成与优化，不参与边缘检测核心算法的编写。

系统的核心算法重点包括：灰度化、二维卷积、Sobel / Prewitt / Roberts 等算子响应计算、Canny 的高斯平滑、梯度计算、非极大值抑制、双阈值判断和滞后连接。拓展页面中的 TEED / HED 仅作为深度学习边缘检测原理展示，不计入传统边缘检测核心算法保证范围。

通过本系统，可以完整展示图像边缘检测从传统算子计算、Canny 多阶段检测，到结果对比、过程动画和应用实践的完整链路。
