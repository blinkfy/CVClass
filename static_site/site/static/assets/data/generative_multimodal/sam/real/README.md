# SAM Raster Samples

Generated PNG copies of the SAM classroom SVG samples go here.

The browser page can display the existing SVG fallback, but the real SAM
Decoder-only path should use PNG files whose offline embeddings were generated
from the same pixels.
